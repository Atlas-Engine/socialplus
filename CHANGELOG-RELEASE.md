- Friends on Classic Anniversary now show "TBC" and their region instead of the full "WoW Classic Anniversary - <realm>" text, which ran past the end of the row.
- The arena marker now works on French and Spanish clients. Accented zone names never matched, so it had never appeared in those languages.
- Scroll speed is no longer left unset on a fresh install.
- Turning SocialPlus notifications off now restores Blizzard's own friend online/offline pop-ups to the setting you had, instead of switching them on.
- The friend tooltip now follows the row under your cursor when the list rebuilds and the mouse has not moved.
- Fewer spurious notifications just after logging in: the delay meant to hold them back until the first scans settled was not being applied.
- Lower memory use across a long session; an internal text cache grew without limit.


View all changelogs: https://github.com/Atlas-Engine/socialplus/blob/main/CHANGELOG.md
